// File extension for OpenDocs documents
export const OPENDOCS_EXTENSION = '.opendocs.json';

// Default board dimensions
export const DEFAULT_BOARD_WIDTH = 100; // percentage
export const DEFAULT_BOARD_HEIGHT = 400; // pixels

// Editor placeholder text
export const EDITOR_PLACEHOLDER = 'Start typing, or press "/" for commands...';

// Debounce delays
export const BOARD_SAVE_DEBOUNCE_MS = 500;
export const EDITOR_SAVE_DEBOUNCE_MS = 300;

// Table defaults
export const DEFAULT_TABLE_ROWS = 3;
export const DEFAULT_TABLE_COLS = 3;

// Application name
export const APP_NAME = 'OpenDocs';

// File types for import
export const ACCEPTED_FILE_TYPES = {
  opendocs: '.opendocs.json,.json',
  docx: '.docx',
};
